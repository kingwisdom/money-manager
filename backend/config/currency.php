<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Currency
    |--------------------------------------------------------------------------
    |
    | The currency symbol used when a new user registers. Set it per site via
    | APP_CURRENCY in your .env file (e.g. "$", "€", "£", "₦").
    |
    */

    'default' => env('APP_CURRENCY', '$'),

];
